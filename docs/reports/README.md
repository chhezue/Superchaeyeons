# Reports — 작업 보고서

Must Have급(3파일 이상·API·DB) 작업을 끝낼 때 G4에서 남기는 보고서가 쌓이는 곳이다. `{{문서 루트}}/reports/` 슬롯 기본값. 파일명 `{YYYY-MM-DD}-{kebab-topic}.md`, 골격은 [`templates/reference-doc.md`](../templates/reference-doc.md) "작업 보고서" 절. **"검증하지 못한 것" 절**이 핵심이다 — 환경이 없어 못 돌린 검증은 실패가 아니라 여기에 남기는 것이 정상 경로다.
