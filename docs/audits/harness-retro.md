# Harness Retro — 하네스 개선 후보

`retro` 스킬이 승인 후 여기에 append 한다. **코드·설계** 개선은 여기가 아니라 `core-followup.md` 💡 후속 제안이 담당한다.

| 날짜 | 증상 (같은 실수 2회+) | 후보 조치 | 상태 |
|------|----------------------|-----------|------|
| 2026-09-08 | **다른 저장소 문서가 파일 단위로 남아 현재형으로 서술된다.** [감사](../reports/2026-09-07-harness-audit-baseline.md) `ROT-5`(`architecture-diagrams.md`가 통째로 원본 프로젝트 것 — 없는 배포 인프라를 "사실에 기반해 시각화했습니다"와 함께)·`ROT-3`(없는 검증 파이프라인을 현재형으로). 하네스를 옮겨 붙인 다른 저장소에서도 같은 사고 보고 | [`doc-reviewer`](../../.claude/agents/doc-reviewer.md) 1단계에 **소유권 축** 추가 — 제목·고유명사가 이 저장소에 실재하는가 · 이력인가 지시인가 · 이력 문서에 배너가 있는가. 값 단위 드리프트(3단계)와 파일 단위 오염을 가른다 | 반영(2026-09-08) |
| 2026-09-08 | **한 절만 고치고 나머지를 안 봐서 문서가 자기 자신과 모순된다.** 감사 `ROT-2`(같은 문서 51행 `52,000B` vs 105행 `65,000B`)·`ROT-8`(스펙이 자기 결정 이력과 어긋남). 값이 두 문서에 갈린 것보다 발견이 늦다 | [`preflight`](../../.claude/skills/preflight/SKILL.md) 5단계 레거시 재점검에 **"고친 문서 안을 먼저 본다"** 추가 — 상수·개수를 바꿨으면 그 숫자로 저장소를 검색. `doc-reviewer` 심각도 표에도 "한 문서 안에서 같은 값을 다르게 말함"을 반드시 고칠 것으로 | 반영(2026-09-08) |
| 2026-09-08 | **규칙·템플릿이 "이렇게 쓰라"고 준 예시가 실물과 다르다.** 감사 `ROT-7`(배달 템플릿의 실패 응답 예시가 다른 프로젝트 도메인어). 다른 저장소에서는 규칙이 지시한 예시가 **컴파일조차 되지 않았다**. 기존 규칙은 발췌를 갱신 의무에서 빼 주기만 해 이 종류를 덮지 못했다 | [`doc-writing.md`](../../.claude/rules/doc-writing.md) "코드는 복사하지 않고 가리킨다" 절에 **설명용 발췌 / 지시용 예시** 구분 표 추가. 지시용은 면제 없음 — 실물에서 복사하고 대조한다 | 반영(2026-09-08) |
| 2026-09-08 | **없어진 파일로 안내하는 링크를 따라가면 규칙이 없고, 없으면 에이전트가 스스로 판단한다.** 감사에서 깨진 링크 11개를 손으로만 고쳐 재발 방지가 없었다(Top-5 개선 5번 미구현) | [`check-doc-style.sh`](../../scripts/check-doc-style.sh)에 **상대 링크 실존 검사**를 경고 등급으로. 외부 URL·앵커·슬롯은 제외, `examples/`는 검사 대상 자체가 아님. 반영 시점 위반 0건이라 예방 목적 | 반영(2026-09-08) |
| 2026-09-08 | (외부 하네스 회고에서 넘어온 후보) 이식할 때 슬롯만 갈아끼우고 **규칙 본문을 대조하지 않아** 없는 것을 사실처럼 지시함 | 이식 절차에 "본문 전수 대조"를 필수 단계로 명시 | **채택 안 함 — 이미 반영됨.** 부품 계약 `C1~C4`([`check-portability.sh`](../../scripts/check-portability.sh))가 규칙 본문에 프로젝트 사실이 들어오는 것 자체를 커밋 시점에 막고, `adopt` 3단계의 "씨앗 대조"와 산출물 체크리스트 8개가 복사한 씨앗의 전제를 실물과 맞춘다 |
