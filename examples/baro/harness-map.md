# Harness Map — 채운 예 (baro-farm-be · `baro-ai`)

`adopt` 1단계 실측(`scripts/adopt-probe.sh ~/IdeaProjects/baro-farm-be baro-ai`, 2026-09-06)을 근거로 2단계 제안 형태로 채운 견본이다. TripFit 견본(`../tripfit/harness-map.md`)과 네 축이 전부 반대쪽이라, 슬롯·플래그만으로 표현되는지 확인하는 두 번째 케이스다. 실제 baro 저장소의 `harness-map.md`가 아니라 **이 하네스 v2가 baro에 붙었을 때의 모습**이다. 사람이 확인해야 할 값은 `[확인 필요]`로 남겼다.

## 축

| 축 | baro-ai | 실측 근거 |
|----|---------|-----------|
| **lang** | Java · Spring Boot · Gradle — 씨앗 `java-spring` 복사 후 **씨앗 대조 필수**. 알려진 차이: therapi 없음, 에러 코드 기반 타입 다름, 아키텍처 테스트 없음 | `baro-ai/build.gradle`, 확장자 java 85 |
| **shape** | 모노레포 부분 소유 — 루트에 하위 패키지 13개, 하네스 범위는 `baro-ai/` | 깊이 2 빌드 파일 13개, `baro-ai/docs/` 존재, 루트 `.github/`에 CONTRIBUTING 없음(이슈 템플릿만) |
| **deploy** | 게이트웨이 뒤 내부 서비스, k8s 배포. 외부 클라이언트 없음 | `k8s/` 존재, API 계약 스냅샷 없음 |
| **stage** | 기능 완성 후 인수, 운영 환경 있음 | 커밋 1,475 · 태그 4 · 마이그레이션 디렉터리 없음 · 운영 설정 흔적 `[확인 필요]` |

## 경로 슬롯

| 슬롯 | baro-ai 값 |
|------|-----------|
| `{{문서 루트}}` | `baro-ai/docs/` |
| `{{작업 범위}}` | `baro-ai` — `deny-out-of-scope-write.sh`의 `SCOPE="baro-ai"`와 함께 고친다 |
| `{{Git 컨벤션 SSOT}}` | (없음) — 루트 `.github/`는 5인 공용이라 파일을 둘 수 없다 → `core-workflow` G2·G4가 SSOT |
| `{{스펙 저장소}}` | `baro-ai/docs/specs/` |
| `{{현재동작 요약}}` | `[확인 필요]` — 후보 `baro-ai/docs/IMPLEMENTATION.md` |
| `{{우선순위 SSOT}}` | `[확인 필요]` |
| `{{아키텍처 개요}}` | `baro-ai/docs/ELASTICSEARCH_SEARCH_EMBEDDING_ARCHITECTURE.md` |
| `{{스키마 SSOT}}` | `[확인 필요]` — 검색 인덱스 매핑 문서가 후보 |
| `{{API 응답 규격}}` | `[확인 필요]` |
| `{{API 문서}}` | (없음) — 외부 클라이언트 없음 |
| `{{제품 범위}}` | `baro-ai/docs/product/` |
| `{{클라이언트 전제}}` | (없음) — 게이트웨이 내부 서비스 |
| `{{용어집}}` | `baro-ai/docs/product/glossary.md` |
| `{{결정 기록}}` | `baro-ai/docs/decisions/` |
| `{{감사 로그}}` | `baro-ai/docs/audits/` |
| `{{배포 SSOT}}` | `k8s/` `[확인 필요 — baro-ai 매니페스트 경로]` |

## 명령 슬롯

| 슬롯 | baro-ai 값 | 근거 |
|------|-----------|------|
| `{{테스트 명령}}` | `[확인 필요]` — `.github/workflows/baro-ai-deploy.yml`의 실제 단계에서 읽는다 | 실측표 CI 행 |
| `{{빌드 명령}}` | 같은 워크플로 | |
| `{{포맷 명령}}` | (없음) — 포맷 설정 파일 없음 | 실측표 |
| `{{의존성 조회}}` | Gradle 의존성 트리 명령 | 빌드 파일 |

## 형식 슬롯 — 기본값과 다르다

| 슬롯 | baro 실측 | 근거 |
|------|-----------|------|
| `{{커밋 메시지 형식}}` | `[Type] #n - {설명}` — 최근 188건 중 126건. 이슈 번호 없는 `[Type] {설명}`이 36건, 소문자 `type:`이 23건 | `git log --format=%s` 집계. **`commit-msg` 훅 정규식을 이 형식으로 바꿔야 한다** |
| `{{브랜치명 형식}}` | `{type}/{desc}`(16)와 구분자 없음(16)이 반반 — 이슈 번호 없음 | `git branch -r` |

기본값 `{Type}: {한글}`을 물려받으면 첫 커밋부터 훅에 막힌다. 이것이 "관례를 물려받지 않고 실측한다"는 규칙이 있는 이유다. (2026-09-05 스펙은 baro 관례를 `[Type] #n - {한글}`로 적었고, 실측이 그것을 확인했다.)

## 능력 플래그

| 플래그 | baro-ai | 근거 |
|--------|---------|------|
| DB 마이그레이션 금지 | `[확인 필요]` — 운영 데이터가 있으면 ❌ | 마이그레이션 디렉터리는 없지만 운영 환경이 있다 |
| 에러 코드 카탈로그 동시갱신 | ☑ — 씨앗 대조에서 기반 타입을 실물(`BaseErrorCode`)로 고친 뒤 | 회고 3 |
| 권한 게이트·활동 기록 동시갱신 | ❌ `[확인 필요]` — 해당 메커니즘 없음 | |
| API 계약 보호 | ❌ — 외부 클라이언트·계약 스냅샷 없음 | 실측표 |
| 생성 문서 검증 | ☑ — 생성 API 문서를 쓴다면. therapi 전제는 씨앗 대조에서 제거 | 회고 1 |
| 스택 함정 메모 | ☑ — 씨앗 `local-g1-stack-trap.md`를 실물 의존성 버전으로 고쳐 복사 | 회고 2 |
| 스택 리뷰어 | ☑ — 씨앗 `spring-reviewer`, 아키텍처 테스트 제외 목록은 비운다 | 씨앗 대조 |

## 이 견본이 증명하는 것

네 축이 TripFit과 전부 반대인데도 core 규칙·스킬·에이전트·훅 본문은 한 줄도 바뀌지 않는다. 바뀌는 것은 이 파일의 값, 훅 상수 두 개(`SCOPE`·`commit-msg` 정규식), 복사한 씨앗 본문뿐이다. 2026-09-05 이전 v1에서 baro가 `core-*.md`를 포함해 21개 파일을 고쳤던 것과 대비된다.
