---
paths:
  - "**/*.[언어 확장자]"
---

# [스택 이름] — 코딩 규칙

[이 프로젝트가 쓰는 언어·프레임워크·버전을 한 줄로. 실측 근거(빌드 파일 경로)를 함께.] 이 파일은 lang 팩이다 — core 규칙이 `[플래그: X]` 배지로 부르는 절이 아래에 있고, 절 이름은 바꾸지 않는다.

## 패키지 · 모듈 구조

[최상위를 무엇으로 나누는가(도메인/기능/레이어), 새 코드가 들어갈 자리를 정하는 규칙. 전체 트리 SSOT가 따로 있으면 링크만.]

## 레이어 규칙

[진입점·유스케이스·도메인·저장 계층이 각각 하는 일과 하지 않는 일. 정적 검사(아키텍처 테스트·린터)가 이미 잡는 규칙은 표시해 리뷰어가 중복 지적하지 않게.]

## 주석 규칙

[원칙은 `core-code-comments.md`가 SSOT다 — 실행 줄 위 `[N단계: …]` 주석, 필드·의존성 해설 생략 금지, 실물 대조, 이유 평문. 여기는 이 언어의 **표기**만 적는다: 문서화 주석 문법(예: `/** */`·docstring·JSDoc), 필드 주석 위치, API 스키마 표기가 필드 설명 SSOT인 경우 그 방식.]

## 테스트 규칙

- 실행: `{{테스트 명령}}`
- 위치·네이밍: [ ]
- "유의미한 테스트만": [무엇을 테스트하고 무엇을 하지 않는가]

## 같은 턴 즉시 갱신 — 에러 코드 · 권한 게이트·활동 기록

> **[플래그: 에러 코드 카탈로그 동시갱신]** · **[플래그: 권한 게이트·활동 기록 동시갱신]** — `harness-map.md`에서 꺼져 있으면 해당 행을 무시한다. 절은 삭제하지 않는다. 원칙은 `core-guardrails.md` STOP §1.7이고, 여기는 이 스택에서 그 원칙이 무엇을 뜻하는지 적는다.

| 변경 | 같은 턴에 필수 | 플래그 |
|------|----------------|--------|
| 새 실패 분기 | [에러 코드 카탈로그 위치] + 예외 throw + 스펙 에러 표 + 응답 스키마 | 에러 코드 카탈로그 동시갱신 |
| 폐기된 실패 분기 | 카탈로그·throw·스펙·생성 문서 **삭제** (`core-guardrails.md` STOP §2) | 에러 코드 카탈로그 동시갱신 |
| 권한이 필요한 진입점 | [권한 게이트 메커니즘 — 데코레이터·미들웨어·인터셉터 등] | 권한 게이트·활동 기록 동시갱신 |
| 활동 시각 기록 | [기록 메커니즘] | 권한 게이트·활동 기록 동시갱신 |

## DB 스키마 정책

> **[플래그: DB 마이그레이션 금지]** [운영 보존 데이터가 없어 스키마 SSOT가 모델 정의 하나인가(☑), 마이그레이션 도구가 SSOT인가(❌). ❌면 이 절에는 마이그레이션 파일의 위치·네이밍·검토 규칙을 적는다.]

## API 계약 변경

> **[플래그: API 계약 보호]** [계약 스냅샷 파일 · diff 도구 · 커밋 사유 트레일러 · 프론트 알림 흐름. 외부 클라이언트가 없으면 "이 프로젝트엔 없음"과 이유.]

## 생성 문서 검증

> **[플래그: 생성 문서 검증]** [소스 표기가 있어도 생성 API 문서에서 사라지는 이 스택의 함정과, 실제 생성물을 여는 방법(로컬 URL·빌드 산출물).]

## 스택 함정 메모

> **[플래그: 스택 함정 메모]** [웹 예제 대다수가 다른 주 버전 기준인 지점, 공식 문서 URL로 버전을 고정할 수 없는 경우 등 — 한 문단.]
