# 참조 문서 템플릿

값·계약·목록을 **정확히 확인하러 오는** 독자를 위한 문서를 쓸 때 이 구조를 복사해서 채운다. 이 저장소에서는 ERD·API envelope·용어 사전·BR 목록·MVP In/Out이 여기에 해당한다.

독자는 이미 배경을 알고 있고 **특정 값 하나**를 찾으러 온다. 그러므로 설명보다 **찾기 쉬운 구조**가 우선이다 — 표를 먼저 두고, 서술은 표가 답하지 못하는 것만 남긴다.

작성 기준: [`.claude/rules/doc-writing.md`](../../.claude/rules/doc-writing.md)

---

```markdown
# [무엇의 참조인가]

[개요 — 이 문서가 어떤 값들의 SSOT인지, 언제 열어보는지 2~3문장. 다른 문서와의 관계(무엇이 이 문서를 근거로 삼는지)도 여기서 밝힌다.]

## 언제 이 문서를 보는가

- [상황 1 — 예: 새 엔티티를 추가하기 전에 기존 컬럼 규칙을 확인할 때]
- [상황 2]

## [항목 목록]

| 항목 | 값 | 설명 | 비고 |
|------|-----|------|------|
| | | | |

[표에 담기지 않는 예외·조건만 표 아래에 짧게. 표에 이미 있는 내용을 산문으로 반복하지 않는다.]

## 예시

[실제 값이 들어간 예시 1~2개. 형식만 보여주는 빈 껍데기 대신 이 저장소에서 실제로 쓰이는 값을 쓴다.]

## 관련 문서

- [이 문서를 근거로 삼는 문서]
- [이 문서가 근거로 삼는 상위 결정 — `docs/decisions/`]
```

## 작업 산출물로 쓸 때 — 작업 보고서 (`{{문서 루트}}/reports/{YYYY-MM-DD}-{kebab-topic}.md`)

Must Have급(3파일 이상·API·DB) 작업을 끝낼 때 G4에서 남기는 보고서다. 참조 유형이다 — 나중에 "그때 무엇을 검증했고 무엇을 못 했나"를 값으로 확인하러 오기 때문이다. **"검증하지 못한 것" 절이 핵심**이다. 환경이 없어 못 돌린 검증은 실패가 아니라 이 절에 남기는 것이 정상 경로고, 여기 적힌 항목이 다음 작업의 입력이 된다.

```markdown
# {작업 제목} — 작업 보고서

[개요 — 무엇을 왜 했고 결과가 어떤 상태인지 2문장.]

## 무엇을 했나

- [변경 파일·범위를 항목으로]

## 결정

| 결정 | 근거 | 대안 |
|------|------|------|
| | | |

## 검증

| 검증 | 실행한 명령 | 결과 |
|------|-------------|------|
| | | |

## 검증하지 못한 것

| 무엇 | 왜 못 했나 | 누가·언제 확인하나 |
|------|-----------|-------------------|
| | | |

## 남은 것

- [후속 제안·미룬 항목 — `core-followup.md` 형식]
```

## 체크리스트

- [ ] H1 바로 아래에 개요가 있다 (표부터 시작하지 않는다)
- [ ] 표가 서술보다 앞에 온다
- [ ] 같은 값이 다른 문서에도 적혀 있지 않다 — 있으면 한쪽을 링크로 바꾼다
- [ ] 용어가 [`glossary.md`](#)와 일치한다
- [ ] 값이 바뀔 때 **이 문서만 고치면 되는** 상태다 (중복 없음)
