# Audits — 감사·리팩터 기록

`safe-refactor`(B 트랙)와 `retro`(하네스 회고)의 출력 위치. `{{감사 로그}}` 슬롯의 기본값.

| 파일 | 쓰는 스킬 |
|------|-----------|
| `{domain}/{NNN}-{kebab-topic}.md` | `safe-refactor` — 감사 결과. 작업 단위 안에서 `001`부터 증가, 주제를 파일명에 (문제 해결 유형) |
| `{domain}/refactor-log.md` | `safe-refactor` — 반영 이력 append |
| `harness-retro.md` | `retro` — 하네스 자체 개선 후보 |
